interface:
  display_name: "Skill Retrospective"
  short_description: "Review real skill runs and propose focused improvements"
  default_prompt: "Use $skill-retrospective to review this completed skill run with an independent subagent and separate script candidates from prompt changes."

policy:
  allow_implicit_invocation: false
